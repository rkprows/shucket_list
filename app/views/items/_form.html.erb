
  <%= form_for [@list, @item] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_field :description %>

  <% if @item.persisted? %>
    <%= f.label :complete %>
    <%= f.check_box :complete %>
  <% end %>

  <%= f.submit %>
<% end %>

  